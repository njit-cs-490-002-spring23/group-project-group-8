/* eslint-disable prettier/prettier */
import React from 'react';
import ReactDOM from 'react-dom';
import PaddleGame from './paddleGame';

ReactDOM.render(<PaddleGame />, document.getElementById('root'));
